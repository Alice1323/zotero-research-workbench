# AI job plans require confirmation before side effects

v0.3 will let the AI interpret natural-language research requests and produce an AI Job Plan, but it will not automatically run external discovery, bulk provider calls, document imports, or Zotero writes without user confirmation. This keeps the workflow fast and conversational while preserving explicit control over costly requests, external source use, and Zotero data mutations.
